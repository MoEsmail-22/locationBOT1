# 01 - الصلاحيات ورسالة التحميل

## الهدف

البوت الآن يعمل بهذا النظام:

1. الأدمن فقط يستطيع رفع ملف Excel.
2. الأدمن يستطيع البحث أيضا.
3. المستخدم العادي لا يستطيع استخدام البوت إلا إذا كان رقمه موجودا في `ALLOWED_USER_IDS`.
4. المستخدم الموجود في `ALLOWED_USER_IDS` يستطيع البحث فقط.
5. عند رفع ملف Excel، البوت يعرض رسالة تحميل وتحديثات أثناء القراءة والحفظ.

## متغيرات البيئة المطلوبة

ضع هذه القيم في السيرفر أو في ملف `.env` المحلي:

```env
BOT_TOKEN=telegram_bot_token
DATABASE_URL=supabase_postgres_connection_string
ADMIN_IDS=123456789,987654321
ALLOWED_USER_IDS=111222333,444555666
```

## معنى كل متغير

`ADMIN_IDS`

أرقام حسابات تيليجرام للأدمن. الأدمن يستطيع:

- رفع ملف Excel
- البحث
- عرض الإحصائيات
- معرفة رقمه باستخدام `رقمي` أو `/myid`

`ALLOWED_USER_IDS`

أرقام حسابات تيليجرام للمستخدمين المسموح لهم بالبحث فقط.

المستخدم الموجود هنا يستطيع:

- البحث برقم الهاتف
- البحث باسم العميل

لكنه لا يستطيع:

- رفع ملف Excel
- عرض الإحصائيات

## كيف تعرف Telegram ID لأي مستخدم

1. شغل البوت.
2. اطلب من الشخص إرسال:

```text
رقمي
```

أو:

```text
/myid
```

3. البوت سيرد برقم الحساب.
4. ضع الرقم في `ADMIN_IDS` أو `ALLOWED_USER_IDS`.

## مثال

لو الأدمن رقمه:

```text
100100100
```

والمستخدمين المسموح لهم:

```text
200200200
300300300
```

اجعل البيئة هكذا:

```env
ADMIN_IDS=100100100
ALLOWED_USER_IDS=200200200,300300300
```

## رسالة التحميل عند رفع Excel

عند رفع الملف، البوت سيعرض مراحل:

```text
جاري تحميل الملف وقراءة البيانات...
```

ثم:

```text
تم تحميل الملف. جاري تحليل ملف Excel...
```

ثم:

```text
تم العثور على 1239 عميل. جاري الحفظ في قاعدة البيانات...
```

ثم النتيجة:

```text
تم استيراد 100 عميل جديد وتحديث 1139 عميل.
```

## ملاحظة مهمة

إذا كان `ALLOWED_USER_IDS` فارغا، فلن يستطيع المستخدمون العاديون البحث. الأدمن فقط سيستطيع استخدام البوت.
